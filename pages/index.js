import { useAuth } from "@/hooks/useAuth";
import useStoreState from "@/hooks/useStore";

const Home = () => {
  return (
    <>
      <h1 style={{ background: "pink", height: "100vh" }}>HOME</h1>
    </>
  );
};

export default Home;
